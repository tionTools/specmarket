Patch 87 — Prom «Оплатити частинами»: плашка в шапке и блоке доставки

База main:
53864146744f9e4bdc0db729a165320a58732c89

Patch file:
87.patch

Patch SHA-256:
39d5b675e9a986e87a41abff5e08273f2c590061e834bd792cb5bacab8670fb6

Источник признака:
- только реальный Prom API marker `payment_data.rpay_parts`;
- без распознавания текста payment method;
- после первого подтверждённого marker `true` сохраняется при последующих sync, если сокращённый Prom payload не прислал `rpay_parts`.

Что делает Patch 87:
- сохраняет `delivery.isInstallmentPayment` для Prom-заказов с `payment_data.rpay_parts`;
- в строке/шапке заказа вместо обычной Prom-плашки «Оплачено» показывает «Оплачено частями» с маленькой фиолетово-жёлтой пиктограммой;
- такую же плашку показывает в заголовке блока «Доставка»;
- обычные Prom-заказы продолжают показывать обычное «Оплачено»;
- ошибка оплаты и остальные существующие статусы не меняются;
- расчёт роялти, источник заказа и Patch 86 не меняются.

Scope — ровно 5 файлов:
- src/features/orders/types.ts
- src/pages/HomeView.vue
- supabase/functions/_shared/prom-order-financials.ts
- supabase/functions/_shared/prom-order-financials_test.ts
- supabase/functions/sync-prom-orders/index.ts

Не менять:
- Android;
- БД/migrations;
- расчёт роялти/commission;
- определение source/Сайт/Пром;
- другие marketplace sync;
- AGENTS.md;
- credentials/diagnostic routes.

Предпубликационная проверка:
- актуальный origin/main подтверждён: `53864146744f9e4bdc0db729a165320a58732c89`;
- Patch 87 пересобран реальным `git diff`, а не вручную;
- `git apply --check` — PASS;
- фактический `git apply` — PASS;
- `git diff --check` — PASS;
- `git diff --name-only` — ровно 5 заявленных scope-файлов;
- итоговый diff полностью просмотрен;
- опубликованный `87.patch` byte-for-byte совпадает с проверенным: Git blob SHA `53d8dabbc68871494dc60638383cdae8adb0e9a8`;
- SHA-256 опубликованного файла: `39d5b675e9a986e87a41abff5e08273f2c590061e834bd792cb5bacab8670fb6`.

Codex:
1. НЕ трогай основной dirty worktree. Работай в отдельном чистом worktree от origin/main.
2. Fetch refs и проверь, что origin/main всё ещё ровно `53864146744f9e4bdc0db729a165320a58732c89`. Если main изменился — STOP; Patch 87 вручную не адаптировать.
3. Прочитай AGENTS.md. Не изменять AGENTS.md.
4. Получи актуальный `87.patch` byte-for-byte из origin/chatgpt-patches и проверь SHA-256 `39d5b675e9a986e87a41abff5e08273f2c590061e834bd792cb5bacab8670fb6`. Несовпадение => STOP.
5. Выполни `git apply --check 87.patch`. Ошибка => STOP без ручного исправления/--reject/редизайна.
6. Примени Patch. `git diff --name-only` должен показать ровно 5 scope-файлов выше.
7. Если Deno доступен без изменения tooling — запусти `deno test supabase/functions/_shared/prom-order-financials_test.ts`. Если тот же Windows Access denied — не чинить Deno, просто зафиксировать.
8. Запусти штатные frontend проверки: format, type-check, lint, build. Android harness не запускать.
9. Финально: `git diff --check`, полный просмотр diff, scope ровно 5 файлов. Проверить по коду:
   - rpay_parts => isInstallmentPayment=true;
   - без rpay_parts и без ранее подтверждённого marker => false;
   - installment => «Оплачено частями» в шапке и «Доставка»;
   - обычный оплаченный Prom => прежнее «Оплачено»;
   - royalty/source logic не изменены.
10. Перед commit снова fetch origin/main; если база изменилась — STOP.
11. `git add` только 5 scope-файлов. `git add .` запрещён. Commit: `Show Prom installment payment badges`.
12. Push в main только через существующий GitHub SSH. При проблеме SSH — STOP, без HTTPS/browser auth/new credentials.
13. После push отдельно deploy только Supabase Edge Function `sync-prom-orders`. DB push/migrations не выполнять. Frontend deploy — обычный GitHub Actions.
14. Для уже существующего заказа №427999580 новый marker появится после обычной синхронизации этого заказа из Prom; не создавать diagnostic route.
15. Итоговый отчёт краткий: commit SHA, 5 файлов, checks, deploy `sync-prom-orders`, frontend workflow/status. Android workflow не нужен.
