Продолжение Patch 64 после ошибки Gradle Wrapper:
`Could not create parent directory for lock file C:\.gradle\wrapper\dists\...`

Работай только с уже применённым Patch 64 в локальном проекте:
C:\Users\Вадим\Documents\CRM моя\specmarket

ВАЖНО:
- Patch 64 уже применён. НЕ применяй 64.patch повторно.
- Working tree НЕ сбрасывай.
- Keystore уже создан и проверен вне репозитория. НЕ регенерируй его.
- Все четыре GitHub release Secrets уже настроены. НЕ удаляй, НЕ меняй и НЕ перезаписывай их.
- Файлы проекта ради этой инфраструктурной ошибки НЕ меняй.
- Предыдущий временный `S:` mapping уже удалён — это нормально.

Причина текущей остановки: Gradle Wrapper попытался использовать недоступный `C:\.gradle`. Это не ошибка Android-кода. На все локальные Gradle-запуски ниже явно задай writable ASCII Gradle User Home.

1. Проверь текущее состояние
В исходном repo выполни:
- `git diff --check`
- `git diff --name-only`

В diff должны быть только 6 файлов Patch 64:
- `.github/workflows/android-apk.yml`
- `android/app/build.gradle.kts`
- `android/app/src/main/java/ua/orders/crm/Appearance.kt`
- `android/app/src/main/java/ua/orders/crm/MainActivity.kt`
- `android/app/src/main/java/ua/orders/crm/OrderModels.kt`
- `android/app/src/test/java/ua/orders/crm/OrderModelsTest.kt`

Посторонние изменения не трогай и не включай в commit.

2. Подготовь ASCII TEMP/TMP и writable GRADLE_USER_HOME
Из исходного repo:
```powershell
$jtmp = 'C:\jtmp'
$gradleHome = 'C:\jtmp\gradle-home'
New-Item -ItemType Directory -Force -Path $jtmp | Out-Null
New-Item -ItemType Directory -Force -Path $gradleHome | Out-Null

# Явно докажи, что каталог writable, до запуска Gradle.
$writeProbe = Join-Path $gradleHome 'write-test.tmp'
'SpecMarket' | Set-Content -Encoding ASCII $writeProbe
if (!(Test-Path $writeProbe)) { throw 'GRADLE_USER_HOME is not writable.' }
Remove-Item -Force $writeProbe

$env:TEMP = $jtmp
$env:TMP = $jtmp
$env:GRADLE_USER_HOME = $gradleHome
```

Проверь только путь, без вывода секретов:
```powershell
Write-Host "GRADLE_USER_HOME=$env:GRADLE_USER_HOME"
```
Он обязан быть ровно `C:\jtmp\gradle-home`, а не `C:\.gradle`.

3. Подготовь существующую release-подпись
```powershell
$repoRoot = (Get-Location).Path
$signingDir = Join-Path (Split-Path -Parent $repoRoot) 'android-signing'
$keystore = Join-Path $signingDir 'orders-release.jks'
$credentialsFile = Join-Path $signingDir 'orders-release-credentials.txt'
if (!(Test-Path $keystore) -or !(Test-Path $credentialsFile)) { throw 'Existing release signing files not found; stop without regenerating them.' }
$cfg = (Get-Content -Raw $credentialsFile | ConvertFrom-StringData)
$env:ORDERS_RELEASE_STORE_FILE = $keystore
$env:ORDERS_RELEASE_STORE_PASS = $cfg.password
$env:ORDERS_RELEASE_ALIAS = $cfg.alias
$env:ORDERS_RELEASE_KEY_PASS = $cfg.password
```
Не выводи значения паролей/секретов.

4. Создай временный ASCII-путь к ЭТОМУ ЖЕ repo
Не копируй проект.
```powershell
$drive = 'S:'
if (Test-Path "$drive\\") { throw 'S: already exists; choose another free drive letter and use it consistently.' }
cmd /c "subst $drive `"$repoRoot`""
if (!(Test-Path "$drive\\android")) { throw 'subst mapping failed.' }
```

5. Сначала проверь сам Gradle Wrapper
Из ASCII-path:
```powershell
Push-Location S:\android
.\gradlew.bat --no-daemon --version
$wrapperExit = $LASTEXITCODE
Pop-Location
if ($wrapperExit -ne 0) { throw "Gradle wrapper --version failed with exit code $wrapperExit" }
```

Если wrapper снова пытается писать в `C:\.gradle`, остановись и сообщи это: НЕ продолжай tests/build и НЕ меняй проект. Если он использует `C:\jtmp\gradle-home`, продолжай.

6. Debug tests/lint/build
```powershell
Push-Location S:\android
.\gradlew.bat --no-daemon testDebugUnitTest lintDebug assembleDebug --stacktrace
$debugExit = $LASTEXITCODE
Pop-Location
if ($debugExit -ne 0) { throw "Debug Gradle checks failed with exit code $debugExit" }
```

Если появляется реальная ошибка tests/lint/build — стоп без commit/push и сообщи точную ошибку.

Если снова появляется именно `Unable to establish loopback connection`, повтори этот же ASCII-path запуск один раз из обычного системного `powershell.exe`, сохраняя:
- `TEMP=C:\jtmp`
- `TMP=C:\jtmp`
- `GRADLE_USER_HOME=C:\jtmp\gradle-home`
- `--no-daemon`
Если и там loopback-ошибка — остановись и сообщи stacktrace.

7. Release-сборка и проверка подписи
После успешного пункта 6:
```powershell
Push-Location S:\android
.\gradlew.bat --no-daemon assembleRelease --stacktrace
$releaseExit = $LASTEXITCODE
Pop-Location
if ($releaseExit -ne 0) { throw "Release Gradle build failed with exit code $releaseExit" }
```

Проверь APK из исходного repo:
```powershell
$apksigner = Join-Path $env:ANDROID_HOME 'build-tools\36.0.0\apksigner.bat'
if (!(Test-Path $apksigner)) {
    $apksigner = Get-ChildItem (Join-Path $env:ANDROID_HOME 'build-tools') -Recurse -Filter apksigner.bat -File | Sort-Object FullName -Descending | Select-Object -First 1 -ExpandProperty FullName
}
if (!$apksigner -or !(Test-Path $apksigner)) { throw 'apksigner.bat not found.' }
& $apksigner verify --print-certs "android\app\build\outputs\apk\release\app-release.apk"
if ($LASTEXITCODE -ne 0) { throw 'apksigner verify failed.' }
```

8. Удали только временный `subst`
```powershell
cmd /c "subst S: /D"
```
Если использовал другую букву, удали именно её. `C:\jtmp\gradle-home` можно оставить как локальный кеш; он находится вне repo и не должен попадать в Git.

9. Критерий commit/push
Продолжай только если одновременно успешно прошли:
- Gradle wrapper `--version`;
- `testDebugUnitTest`;
- `lintDebug`;
- `assembleDebug`;
- `assembleRelease`;
- `apksigner verify --print-certs`;
- `git diff --check`;
- diff содержит ровно 6 файлов Patch 64.

10. Commit/push
Повторно выполни:
- `git diff --check`
- `git diff --name-only`
- `git diff --stat`

Затем:
- commit только 6 файлов Patch 64;
- commit message: `Polish Android orders app and stabilize release updates`;
- push в `origin/main`.

11. После push
Проверь Android APK workflow для нового main commit. Он обязан успешно:
- выполнить tests/lint;
- собрать `assembleRelease`;
- пройти `apksigner verify`;
- создать artifact `orders-android-release`.

Скачай artifact и повторно проверь `apksigner verify --print-certs`. Зафиксируй SHA-256 сертификата подписанта.

12. Итоговый отчёт
Сообщи только факты:
- SHA commit в main;
- какие локальные проверки прошли;
- что локальная проверка выполнялась через временный ASCII `subst` path и `GRADLE_USER_HOME=C:\jtmp\gradle-home`;
- GitHub Actions run ID/result;
- artifact `orders-android-release`;
- SHA-256 сертификата подписанта;
- подтверждение, что commit содержит только 6 файлов Patch 64;
- напоминание: переход с 0.4 на 0.5 требует один раз удалить старую версию, установить 0.5 и войти; последующие версии при том же release-ключе и возрастающем versionCode должны обновляться поверх приложения.