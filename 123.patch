diff --git a/supabase/functions/_shared/novapay-running-balance.ts b/supabase/functions/_shared/novapay-running-balance.ts
new file mode 100644
--- /dev/null
+++ b/supabase/functions/_shared/novapay-running-balance.ts
@@ -0,0 +1,142 @@
+function text(value) {
+  return typeof value === 'string' ? value.trim() : ''
+}
+
+function moneyCents(value) {
+  const number = Number(value)
+  return Number.isFinite(number) ? Math.round(number * 100) : null
+}
+
+function aliases(receipt) {
+  if (!Array.isArray(receipt?.providerAliases)) return []
+  return [...new Set(receipt.providerAliases.map(text).filter(Boolean))]
+}
+
+function summaryCents(summary) {
+  if (!summary) return null
+  const opening = moneyCents(summary.opening)
+  const debitTotal = moneyCents(summary.debitTotal)
+  const creditTotal = moneyCents(summary.creditTotal)
+  const closing = moneyCents(summary.closing)
+  if (
+    opening === null ||
+    debitTotal === null ||
+    creditTotal === null ||
+    closing === null ||
+    debitTotal < 0 ||
+    creditTotal < 0
+  )
+    return null
+  if (opening + creditTotal - debitTotal !== closing) return null
+  return { opening, debitTotal, creditTotal, closing }
+}
+
+export function assignBalancesFromAllMovements(receipts, movements, daySummaries) {
+  const result = receipts.map((receipt) => ({ ...receipt }))
+  const movementsByDate = new Map()
+  const invalidDates = new Set()
+  let hasUndatedInvalidMovement = false
+
+  for (const movement of movements) {
+    const date = text(movement?.legacyDate)
+    if (!date) {
+      hasUndatedInvalidMovement = true
+      continue
+    }
+
+    const amountCents = moneyCents(movement?.amount)
+    const timestamp = Date.parse(text(movement?.occurredAt))
+    const direction = movement?.direction
+    if (
+      amountCents === null ||
+      amountCents <= 0 ||
+      !Number.isFinite(timestamp) ||
+      (direction !== 'credit' && direction !== 'debit')
+    ) {
+      invalidDates.add(date)
+      continue
+    }
+
+    movementsByDate.set(date, [
+      ...(movementsByDate.get(date) ?? []),
+      { movement, amountCents, timestamp, direction },
+    ])
+  }
+
+  // An undated account movement cannot be assigned to a day, so the completeness
+  // of every daily movement set is unknown. Prefer no balances to fabricated ones.
+  if (hasUndatedInvalidMovement) return result
+
+  const balanceByAlias = new Map()
+  for (const [date, dayMovements] of movementsByDate) {
+    if (invalidDates.has(date)) continue
+
+    const summary = summaryCents(daySummaries.get(date))
+    if (!summary) continue
+
+    let movementDebitCents = 0
+    let movementCreditCents = 0
+    for (const entry of dayMovements) {
+      if (entry.direction === 'credit') movementCreditCents += entry.amountCents
+      else movementDebitCents += entry.amountCents
+    }
+
+    // GetAccountExtract is the authoritative daily aggregate. If GetPaymentsList
+    // does not account for every debit and credit, exact per-payment balances
+    // cannot be reconstructed safely.
+    if (
+      movementDebitCents !== summary.debitTotal ||
+      movementCreditCents !== summary.creditTotal
+    )
+      continue
+
+    const ordered = [...dayMovements].sort((left, right) => right.timestamp - left.timestamp)
+    const duplicateTimestamp = ordered.some(
+      (entry, index) => index > 0 && entry.timestamp === ordered[index - 1].timestamp,
+    )
+    if (duplicateTimestamp) continue
+
+    let balanceAfterCents = summary.closing
+    for (const entry of ordered) {
+      if (entry.direction === 'credit') {
+        for (const alias of aliases(entry.movement)) {
+          balanceByAlias.set(alias, balanceAfterCents / 100)
+        }
+        balanceAfterCents -= entry.amountCents
+      } else {
+        balanceAfterCents += entry.amountCents
+      }
+    }
+
+    if (balanceAfterCents !== summary.opening) {
+      for (const entry of ordered) {
+        for (const alias of aliases(entry.movement)) balanceByAlias.delete(alias)
+      }
+    }
+  }
+
+  return result.map((receipt) => {
+    for (const alias of aliases(receipt)) {
+      if (balanceByAlias.has(alias)) {
+        return { ...receipt, balance: balanceByAlias.get(alias) }
+      }
+    }
+    return receipt
+  })
+}
+
+export function copyKnownBalancesByProviderAlias(receipts, statementReceipts) {
+  const balanceByAlias = new Map()
+  for (const receipt of statementReceipts) {
+    const balance = receipt?.balance
+    if (typeof balance !== 'number' || !Number.isFinite(balance)) continue
+    for (const alias of aliases(receipt)) balanceByAlias.set(alias, balance)
+  }
+
+  return receipts.map((receipt) => {
+    for (const alias of aliases(receipt)) {
+      if (balanceByAlias.has(alias)) return { ...receipt, balance: balanceByAlias.get(alias) }
+    }
+    return receipt
+  })
+}
diff --git a/supabase/functions/_shared/novapay-running-balance_test.ts b/supabase/functions/_shared/novapay-running-balance_test.ts
new file mode 100644
--- /dev/null
+++ b/supabase/functions/_shared/novapay-running-balance_test.ts
@@ -0,0 +1,170 @@
+import {
+  assignBalancesFromAllMovements,
+  copyKnownBalancesByProviderAlias,
+} from './novapay-running-balance.ts'
+
+function assertEquals(actual, expected, message) {
+  if (actual !== expected) throw new Error(`${message}: ${actual} !== ${expected}`)
+}
+
+const productionSummaries = new Map([
+  [
+    '18.09.2026',
+    { opening: 5999.49, debitTotal: 0, creditTotal: 1248.72, closing: 7248.21 },
+  ],
+  [
+    '19.09.2026',
+    { opening: 7248.21, debitTotal: 0, creditTotal: 752.22, closing: 8000.43 },
+  ],
+])
+
+const productionReceipts = [
+  {
+    legacyDate: '19.09.2026',
+    occurredAt: '2026-09-19T16:47:04.000Z',
+    amount: 626.85,
+    balance: null,
+    providerAliases: ['id:later'],
+  },
+  {
+    legacyDate: '19.09.2026',
+    occurredAt: '2026-09-19T10:56:25.000Z',
+    amount: 125.37,
+    balance: null,
+    providerAliases: ['id:earlier'],
+  },
+  {
+    legacyDate: '18.09.2026',
+    occurredAt: '2026-09-18T16:55:54.000Z',
+    amount: 1248.72,
+    balance: null,
+    providerAliases: ['id:previous-day'],
+  },
+]
+const productionMovements = productionReceipts.map((receipt) => ({
+  ...receipt,
+  direction: 'credit',
+}))
+
+const productionResult = assignBalancesFromAllMovements(
+  productionReceipts,
+  productionMovements,
+  productionSummaries,
+)
+assertEquals(productionResult[0].balance, 8000.43, 'latest 19.09 credit gets day close')
+assertEquals(productionResult[1].balance, 7373.58, 'earlier 19.09 credit gets running balance')
+assertEquals(productionResult[2].balance, 7248.21, 'single 18.09 credit keeps day close')
+
+const debitSummaries = new Map([
+  ['20.09.2026', { opening: 1000, debitTotal: 50, creditTotal: 300, closing: 1250 }],
+])
+const debitReceipts = [
+  {
+    legacyDate: '20.09.2026',
+    occurredAt: '2026-09-20T09:00:00.000Z',
+    amount: 200,
+    balance: null,
+    providerAliases: ['id:credit-2'],
+  },
+  {
+    legacyDate: '20.09.2026',
+    occurredAt: '2026-09-20T07:00:00.000Z',
+    amount: 100,
+    balance: null,
+    providerAliases: ['id:credit-1'],
+  },
+]
+const debitMovements = [
+  { ...debitReceipts[0], direction: 'credit' },
+  {
+    legacyDate: '20.09.2026',
+    occurredAt: '2026-09-20T08:00:00.000Z',
+    amount: 50,
+    providerAliases: ['id:debit-1'],
+    direction: 'debit',
+  },
+  { ...debitReceipts[1], direction: 'credit' },
+]
+const debitResult = assignBalancesFromAllMovements(debitReceipts, debitMovements, debitSummaries)
+assertEquals(debitResult[0].balance, 1250, 'later credit balance after intervening debit')
+assertEquals(debitResult[1].balance, 1100, 'earlier credit restores intervening debit')
+
+const totalsMismatch = assignBalancesFromAllMovements(
+  debitReceipts,
+  debitMovements,
+  new Map([
+    ['20.09.2026', { opening: 990, debitTotal: 60, creditTotal: 300, closing: 1230 }],
+  ]),
+)
+assertEquals(totalsMismatch[0].balance, null, 'daily aggregate mismatch does not invent balance')
+assertEquals(totalsMismatch[1].balance, null, 'aggregate mismatch suppresses whole day')
+
+const ambiguousTimestamp = assignBalancesFromAllMovements(
+  debitReceipts,
+  [
+    { ...debitReceipts[0], direction: 'credit' },
+    {
+      legacyDate: '20.09.2026',
+      occurredAt: debitReceipts[0].occurredAt,
+      amount: 50,
+      providerAliases: ['id:same-second-debit'],
+      direction: 'debit',
+    },
+    { ...debitReceipts[1], direction: 'credit' },
+  ],
+  debitSummaries,
+)
+assertEquals(
+  ambiguousTimestamp[0].balance,
+  null,
+  'same-second ordering ambiguity does not invent balance',
+)
+
+const invalidMovement = assignBalancesFromAllMovements(
+  debitReceipts,
+  [
+    { ...debitReceipts[0], direction: 'credit' },
+    {
+      legacyDate: '20.09.2026',
+      occurredAt: '',
+      amount: 50,
+      providerAliases: ['id:no-time-debit'],
+      direction: 'debit',
+    },
+    { ...debitReceipts[1], direction: 'credit' },
+  ],
+  debitSummaries,
+)
+assertEquals(invalidMovement[0].balance, null, 'movement without timestamp suppresses day')
+
+const updatedResult = copyKnownBalancesByProviderAlias(
+  [{ providerAliases: ['uetr:shared', 'id:earlier'], balance: null }],
+  productionResult,
+)
+assertEquals(updatedResult[0].balance, 7373.58, 'updated path reuses statement balance')
diff --git a/supabase/functions/novapay-data/index.ts b/supabase/functions/novapay-data/index.ts
--- a/supabase/functions/novapay-data/index.ts
+++ b/supabase/functions/novapay-data/index.ts
@@ -5,6 +5,10 @@ import {
   NovaPayAuthError,
   NovaPayTransportError,
 } from '../_shared/novapay-auth.ts'
+import {
+  assignBalancesFromAllMovements,
+  copyKnownBalancesByProviderAlias,
+} from '../_shared/novapay-running-balance.ts'
 
 const NOVAPAY_URL = 'https://business.novapay.ua/Services/ClientAPIService.svc'
 const SOAP_ACTION_BASE = 'http://tempuri.org/IClientAPIService/'
@@ -226,7 +230,7 @@ function parsePaymentsDocuments(rawXml) {
   return collectNestedRecords(root, 'Docs').map(normalizeXmlRecord)
 }
 
-function parseExtractDayBalances(rawXml) {
+function parseExtractDaySummaries(rawXml) {
   const xml = text(rawXml)
   if (!xml) return new Map()
 
@@ -240,13 +244,22 @@ function parseExtractDayBalances(rawXml) {
   const root = findKey(document, 'Extract')
   if (!isRecord(root)) return new Map()
 
-  const balances = new Map()
+  const summaries = new Map()
   for (const day of collectNestedRecords(root, 'GetExtractForXML')) {
     const statement = normalizeXmlRecord(day)
     const date = pick(statement, 'Date')
-    const balance = optionalNumber(pick(statement, 'OutCome', 'MainOutCome'))
-    if (date && balance !== null) balances.set(date, balance)
+    if (!date) continue
+    summaries.set(date, {
+      opening: optionalNumber(pick(statement, 'InCome', 'MainInCome')),
+      debitTotal: optionalNumber(pick(statement, 'DebitAmount', 'MainDebitAmount')),
+      creditTotal: optionalNumber(pick(statement, 'CreditAmount', 'MainCreditAmount')),
+      closing: optionalNumber(pick(statement, 'OutCome', 'MainOutCome')),
+    })
   }
-  return balances
+  return summaries
 }
 
 function pick(record, ...keys) {
@@ -301,7 +314,7 @@ function parseNovaDateTime(value) {
   }
 }
 
-function extractReceipt(document, dailyBalances = new Map()) {
+function extractReceipt(document) {
   const amount = optionalNumber(
     pick(document, 'Amount', 'CrncyCredit', 'CreditAmount', 'Credit', 'SumCredit'),
   )
@@ -328,7 +341,7 @@ function extractReceipt(document, dailyBalances = new Map()) {
     occurredAt: eventDateTime ? receiptOccurredAt(eventDateTime.date, eventDateTime.time) : '',
     description: pick(document, 'DebitName', 'PayerName', 'SenderName', 'Description'),
     amount,
-    balance: dailyBalances.get(paymentDate) ?? null,
+    balance: null,
     comment: pick(document, 'Purpose', 'Comment', 'Description'),
     legacyDate: paymentDate,
     legacyCreatedDate: createdAt?.date ?? '',
@@ -340,6 +353,52 @@ function extractReceipt(document, dailyBalances = new Map()) {
   }
 }
 
+function extractBalanceMovement(document, accountIban) {
+  const paymentDate = pick(document, 'DayDate', 'OrgDate', 'PaymentDate', 'Date', 'date')
+  const changedAt = parseNovaDateTime(pick(document, 'Changed'))
+  const createdAt = parseNovaDateTime(pick(document, 'Created'))
+  const eventDateTime = changedAt ?? createdAt
+  const amount = optionalNumber(
+    pick(
+      document,
+      'Amount',
+      'CrncyCredit',
+      'CreditAmount',
+      'Credit',
+      'SumCredit',
+      'CrncyDebit',
+      'DebitAmount',
+      'Debit',
+      'SumDebit',
+    ),
+  )
+  const debitIban = normalizeIban(pick(document, 'DebitCodeIBAN', 'DebitIBAN', 'debitIBAN'))
+  const creditIban = normalizeIban(pick(document, 'CreditCodeIBAN', 'CreditIBAN', 'creditIBAN'))
+  const providerId = pick(document, 'ID', 'id')
+  const uetr = pick(document, 'UETR')
+  const providerAliases = [providerId ? `id:${providerId}` : '', uetr ? `uetr:${uetr}` : ''].filter(
+    Boolean,
+  )
+  const isCredit = creditIban === accountIban && debitIban !== accountIban
+  const isDebit = debitIban === accountIban && creditIban !== accountIban
+
+  return {
+    legacyDate: paymentDate,
+    occurredAt: eventDateTime ? receiptOccurredAt(eventDateTime.date, eventDateTime.time) : '',
+    amount,
+    providerAliases,
+    direction: isCredit ? 'credit' : isDebit ? 'debit' : 'unknown',
+  }
+}
+
 function kyivDateTimeParts(utcMs) {
   const parts = new Intl.DateTimeFormat('en-CA', {
     timeZone: 'Europe/Kyiv',
@@ -953,8 +1012,14 @@ function extractReceipt(document, dailyBalances = new Map()) {
       },
       true,
     )
-    const dailyBalances = parseExtractDayBalances(extractResult.extract)
-    dailyBalances.set(dateTo, available)
+    const daySummaries = parseExtractDaySummaries(extractResult.extract)
+    const todaySummary = daySummaries.get(dateTo) ?? {}
+    daySummaries.set(dateTo, {
+      ...todaySummary,
+      closing: available,
+    })
     const statementPaymentsResult = await soapCall(
       'GetPaymentsList',
       {
@@ -979,10 +1044,17 @@ function extractReceipt(document, dailyBalances = new Map()) {
     const statementIncomingDocuments = statementConductedDocuments.filter((document) =>
       isIncomingPaymentDocument(document, accountIban),
     )
-    const receipts = sortReceiptsNewestFirst(
-      statementIncomingDocuments
-        .map((document) => extractReceipt(document, dailyBalances))
-        .filter((receipt) => receipt !== null),
+    const statementMovements = statementConductedDocuments.map((document) =>
+      extractBalanceMovement(document, accountIban),
+    )
+    const receipts = assignBalancesFromAllMovements(
+      sortReceiptsNewestFirst(
+        statementIncomingDocuments
+          .map((document) => extractReceipt(document))
+          .filter((receipt) => receipt !== null),
+      ),
+      statementMovements,
+      daySummaries,
     )
 
     const updatedDocuments = parsePaymentsDocuments(updatedPaymentsResult.payments)
@@ -990,10 +1062,13 @@ function extractReceipt(document, dailyBalances = new Map()) {
     const updatedIncomingDocuments = updatedConductedDocuments.filter((document) =>
       isIncomingPaymentDocument(document, accountIban),
     )
-    const updatedReceipts = sortReceiptsNewestFirst(
-      updatedIncomingDocuments
-        .map((document) => extractReceipt(document, dailyBalances))
-        .filter((receipt) => receipt !== null),
+    const updatedReceipts = copyKnownBalancesByProviderAlias(
+      sortReceiptsNewestFirst(
+        updatedIncomingDocuments
+          .map((document) => extractReceipt(document))
+          .filter((receipt) => receipt !== null),
+      ),
+      receipts,
     )
 
     const confirmed = finiteNumber(balanceResult.confirmed_balance, 'confirmed balance')
