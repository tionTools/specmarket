Patch 53 — ДОБИТЬ ФИНАЛ. Выполняй механически. Никаких исследований, новых правок или установки постоянных инструментов.

Проект:
C:\Users\Вадим\Documents\CRM моя\specmarket

Текущее состояние уже прошло: pnpm format, pnpm type-check, pnpm lint, pnpm build, git diff --check. НЕ повторять эти web-проверки.

ВАЖНО:
- AGENTS.md не трогать и не stage;
- НЕ включать в commit: PATCH53-REVIEW.txt, android/PATCH53-SPEC.md, deno.lock, supabase/migrations/20260907090000_mobile_devices_and_order_outbox.sql;
- миграцию FCM НЕ применять;
- никаких функциональных изменений;
- если тест/проверка кода падает — не исправлять самостоятельно, показать ошибку и остановиться.

1. Deno-проверки. Не искать deno.exe и не ставить Deno глобально. Запустить зафиксированную версию 2.9.6 временно через pnpm dlx:
pnpm dlx deno@2.9.6 test --no-lock supabase/functions/_shared/financials_test.ts supabase/functions/_shared/kasta-return_test.ts supabase/functions/_shared/marketplace-accept_test.ts supabase/functions/_shared/shipment-accounting_test.ts
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
pnpm dlx deno@2.9.6 check --no-lock supabase/functions/sync-epicentr-orders/index.ts supabase/functions/sync-kasta-orders/index.ts
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

2. Android. Ничего не устанавливать. Использовать имеющийся JDK 17.
$Java = Get-Command java -ErrorAction SilentlyContinue
if (!$Java) {
  $Candidates = @(
    'C:\Program Files\Eclipse Adoptium',
    "$env:LOCALAPPDATA\Programs\Eclipse Adoptium",
    'C:\Program Files\Java',
    'C:\Program Files\Microsoft'
  )
  $Jdk = foreach ($Root in $Candidates) {
    if (Test-Path $Root) { Get-ChildItem $Root -Directory -ErrorAction SilentlyContinue | Where-Object { $_.Name -match 'jdk|temurin' } }
  } | Sort-Object LastWriteTime -Descending | Select-Object -First 1
  if (!$Jdk) { throw 'Existing JDK not found. Stop; do not install anything.' }
  $env:JAVA_HOME = $Jdk.FullName
  $env:Path = "$env:JAVA_HOME\bin;$env:Path"
}
java -version

$ProjectRoot = (Get-Location).Path
if (Test-Path 'X:\') { throw 'X: is already in use. Stop.' }
subst X: "$ProjectRoot"
try {
  Push-Location X:\android
  .\gradlew.bat testDebugUnitTest lintDebug assembleDebug --no-daemon
  $AndroidExit = $LASTEXITCODE
  Pop-Location
  if ($AndroidExit -ne 0) { exit $AndroidExit }
} finally {
  Set-Location $ProjectRoot
  subst X: /D
}

git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

3. Stage ТОЛЬКО эти файлы:
git add -- `
  android/app/build.gradle.kts `
  android/app/src/main/java/ua/orders/crm/MainActivity.kt `
  android/app/src/main/java/ua/orders/crm/OrderModels.kt `
  android/app/src/main/java/ua/orders/crm/OrdersRepository.kt `
  android/app/src/main/java/ua/orders/crm/OrdersViewModel.kt `
  android/app/src/main/java/ua/orders/crm/Appearance.kt `
  android/app/src/test/java/ua/orders/crm/OrderModelsTest.kt `
  src/global.css `
  src/layouts/MainLayout.vue `
  src/main.ts `
  src/pages/HomeView.vue `
  src/features/orders/financials.ts `
  src/features/orders/shipment-accounting.ts `
  src/lib/appearance.ts `
  src/lib/physical-shipment.ts `
  supabase/functions/_shared/financials_test.ts `
  supabase/functions/_shared/kasta-return.ts `
  supabase/functions/_shared/kasta-return_test.ts `
  supabase/functions/_shared/marketplace-accept.ts `
  supabase/functions/_shared/marketplace-accept_test.ts `
  supabase/functions/_shared/shipment-accounting_test.ts `
  supabase/functions/sync-epicentr-orders/index.ts `
  supabase/functions/sync-kasta-orders/index.ts

git diff --cached --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

$Staged = git diff --cached --name-only
$Staged
$Forbidden = @('AGENTS.md','PATCH53-REVIEW.txt','android/PATCH53-SPEC.md','deno.lock','supabase/migrations/20260907090000_mobile_devices_and_order_outbox.sql')
foreach ($File in $Forbidden) { if ($Staged -contains $File) { throw "Forbidden staged file: $File" } }

4. Сразу commit + push:
git commit -m "feat: update mobile actions themes and return workflow"
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$Commit = git rev-parse HEAD
git push origin main
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

5. Сразу deploy двух изменённых Edge Functions:
pnpm exec supabase functions deploy sync-epicentr-orders
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
pnpm exec supabase functions deploy sync-kasta-orders
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

НЕ делать db push. НЕ применять migration. НЕ настраивать Firebase. НЕ исследовать Kasta contract. НЕ проверять GitHub Actions.

6. В ответ дай только:
- полный SHA commit;
- Deno: test/check результат;
- Android: testDebugUnitTest/lintDebug/assembleDebug результат;
- push result;
- deploy двух функций;
- git status --short.