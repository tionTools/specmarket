name: Patch 73 registry unpaid validation

on:
  push:
    branches:
      - patch73-registry-unpaid-validation

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build Patch 73 from exact main
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD^)" = '896bad13fe7bc68580e60f3816e990f0b895d570'
          python - <<'PY'
          from pathlib import Path
          path = Path('src/pages/HomeView.vue')
          source = path.read_text(encoding='utf-8')
          old = """    isPromRegistryDraft.value = true
    searchQuery.value = ''
    platformFilter.value = 'all'
    isShowingCancellations.value = false
    isShowingReturns.value = false
"""
          new = """    isPromRegistryDraft.value = true
    searchQuery.value = ''
    platformFilter.value = 'all'
    isShowingCancellations.value = false
    isShowingReturns.value = false
    isShowingUnpaidOnly.value = false
"""
          count = source.count(old)
          if count != 1:
              raise SystemExit(f'Patch 73 anchor count={count}, expected 1')
          path.write_text(source.replace(old, new), encoding='utf-8')
          PY
          git diff --check
          test "$(git diff --name-only)" = 'src/pages/HomeView.vue'
          grep -A6 -F "isPromRegistryDraft.value = true" src/pages/HomeView.vue | grep -F "isShowingUnpaidOnly.value = false" >/dev/null

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install pnpm
        run: npm install --global pnpm@10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Frontend checks
        shell: bash
        run: |
          set -euo pipefail
          pnpm format
          if ! git diff --quiet -- src/features/orders/delivery-tracking.ts; then
            git checkout -- src/features/orders/delivery-tracking.ts
          fi
          pnpm type-check
          pnpm lint
          pnpm build
          git diff --check
          test "$(git diff --name-only)" = 'src/pages/HomeView.vue'
          ! git diff --name-only | grep -Fx 'AGENTS.md'
          ! git diff --name-only | grep -E '^supabase/'
          ! git diff --name-only | grep -E '^android/'

      - name: Normalize and reapply Patch 73
        shell: bash
        run: |
          set -euo pipefail
          git diff --binary --full-index -- src/pages/HomeView.vue > /tmp/73.patch
          test -s /tmp/73.patch
          git reset --hard HEAD
          git clean -fd
          git apply --check /tmp/73.patch
          git apply /tmp/73.patch
          git diff --check
          test "$(git diff --name-only)" = 'src/pages/HomeView.vue'
          pnpm type-check
          pnpm lint
          pnpm build
          git diff --check
          test "$(git diff --name-only)" = 'src/pages/HomeView.vue'

      - uses: actions/upload-artifact@v4
        with:
          name: patch73-registry-unpaid
          path: /tmp/73.patch
          if-no-files-found: error
