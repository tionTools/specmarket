Patch 77 — раздельное сторно USD/грн в сверке

База Patch 77:
8e5986b267d0ddce93fd78b4c5b9a78898db0b84

Работай из репозитория:
C:\Users\Вадим\Documents\CRM моя\specmarket

Не трогай, не stash и не коммить пользовательский AGENTS.md. Используй существующий чистый worktree Patch 77:
C:\Users\Вадим\Documents\CRM моя\specmarket-p77

Если worktree чистый, HEAD = 8e5986b267d0ddce93fd78b4c5b9a78898db0b84 и Patch 77 ещё не применялся — переиспользуй его. Ничего не удаляй и не создавай заново.

1. Обнови refs и заново извлеки Patch 77

git fetch origin main chatgpt-patches

git show origin/chatgpt-patches:77.patch > "$env:TEMP\specmarket-77.patch"

Проверь SHA blob patch-файла:

git hash-object "$env:TEMP\specmarket-77.patch"

Ожидается:
f7e6dd35bb8318da6f6698439cb92b8966c62be8

Если SHA другой — остановись.

2. Проверка базы по Git-объектам

Проверь именно blob SHA трёх изменяемых существующих файлов в HEAD:

git rev-parse HEAD:src/features/reconciliation/ReconciliationHistoryTable.vue
git rev-parse HEAD:src/features/reconciliation/types.ts
git rev-parse HEAD:src/pages/ReconciliationView.vue

Ожидаются соответственно:
8e60795d1d3c2b82fbbf061f2c4be77ecf68ba39
dd211f350a8f237f709ae90896b231011b625a68
066d967c4197d21ad8bd98d0192dc7f7ad08abc4

Эти SHA соответствуют old-index Patch 77. Если любой не совпадает — остановись.

3. Применение Patch 77

Для Patch 77 НЕ использовать обычный `git apply --check`: на локальном Windows worktree он уже дважды дал ложный context failure при совпадающих Git blob SHA.

Используй Git three-way application по old-index blob-объектам patch:

git apply --3way --check "$env:TEMP\specmarket-77.patch"

Если эта проверка прошла:

git apply --3way "$env:TEMP\specmarket-77.patch"

`--3way` может автоматически добавить изменения в index. Сразу после успешного применения только сними staged-состояние, не откатывая файлы:

git reset

Если `git apply --3way --check` не проходит — ничего вручную не исправляй и остановись с полной ошибкой.

4. Patch должен изменить ровно четыре файла

src/pages/ReconciliationView.vue
src/features/reconciliation/types.ts
src/features/reconciliation/ReconciliationHistoryTable.vue
supabase/migrations/20260912100000_reconciliation_usd_adjustment.sql

AGENTS.md, Android, Edge Functions, другие migrations и остальные файлы не менять.

5. Бизнес-логика

- «Сторно грн» меняет только гривневую часть долга.
- При вводе «Сторно USD» поле «Сторно грн» автоматически получает противоположный гривневый эквивалент по текущему курсу CRM, округлённый до копеек:
  сторно_грн = -(сторно_USD × курс).
- Автоматически рассчитанное сторно грн можно вручную изменить.
- После сторно отдельно считаются остаток USD и остаток грн.
- Общий эквивалент считается из скорректированных валютных остатков.
- «Не сходится» показывает отдельно USD, грн и общий гривневый эквивалент.
- В истории две колонки: «Сторно USD» и «Сторно грн».
- Старые записи получают adjustment_usd = 0 и сохраняют существующее adjustment_uah.

Контрольный кейс при курсе 45,20:
до сторно 69,13 USD / 11 614,42 грн;
1С 69,40 USD / 11 602,22 грн;
Сторно USD +0,27 -> Сторно грн автоматически -12,20;
после сторно 69,40 USD / 11 602,22 грн;
расхождения USD, грн и общий итог = 0,00 с учётом округления.

6. Миграция

Patch добавляет только:

alter table public.crm_reconciliations
  add column if not exists adjustment_usd numeric not null default 0;

7. Проверки перед commit

git diff --check
git diff --name-only
git diff --stat
pnpm exec oxfmt src/pages/ReconciliationView.vue src/features/reconciliation/ReconciliationHistoryTable.vue src/features/reconciliation/types.ts
pnpm type-check
pnpm exec oxlint src/pages/ReconciliationView.vue src/features/reconciliation/ReconciliationHistoryTable.vue src/features/reconciliation/types.ts
pnpm exec eslint src/pages/ReconciliationView.vue src/features/reconciliation/ReconciliationHistoryTable.vue src/features/reconciliation/types.ts
pnpm build

После formatter/linter снова проверь:
git status --short
git diff --name-only
git diff --check

Diff должен содержать только четыре файла Patch 77.

Сам patch уже прошёл GitHub Actions: format, type-check, oxlint, eslint, build, scope, git diff --check и git apply --check на чистой базе main.

8. Commit

Не использовать `git add .`.
Добавить только четыре файла Patch 77.

Commit message:
Add currency-aware reconciliation storno

9. Перед push

git fetch origin main

Если origin/main продвинулся — без force-push. Rebase на актуальный origin/main и повтори:
- git diff-tree --no-commit-id --name-only -r HEAD
- git diff --check HEAD^ HEAD
- pnpm type-check
- pnpm build

При конфликте rebase остановись и сообщи.

10. Push

git push origin HEAD:main

Только существующий SSH-доступ GitHub. Не переключаться на HTTPS, не запускать браузерную авторизацию и не создавать credentials. Если SSH push не работает — остановись.

11. Supabase migration

После успешного push:

pnpm exec supabase db push

Используй только уже связанный Supabase project. Если worktree не видит link, возьми существующий project ref из:
C:\Users\Вадим\Documents\CRM моя\specmarket\supabase\.temp\project-ref

Не создавать новый Supabase project и не угадывать project ref. При ошибке db push остановись и пришли её полностью.

12. После deploy

Убедись, что production frontend deploy успешен и migration применена. Старые сверки не удалять и не переписывать.

Для исправления текущего перекоса после выкладки Patch 77 создать ОДНУ новую сверку с актуальными значениями 1С. Если цифры остались прежними:
- Доллары 1С: 69,40
- Гривна 1С: 11 602,22
- Сторно USD: +0,27
- Сторно грн должно автоматически стать -12,20
- USD после сторно: 69,40
- Гривна после сторно: 11 602,22
- отдельные расхождения и общий итог: 0,00
- затем «Зафиксировать сверку».

Если к этому моменту цифры изменились из-за новых заказов, платежей или возвратов — использовать актуальные значения 1С и фактическую текущую валютную разницу, а не старые числа.

13. Итоговый отчёт

Сообщи:
- SHA commit в main;
- SHA origin/main перед применением и перед финальным push;
- что commit содержит ровно четыре файла Patch 77;
- результаты diff-check/type-check/linters/build;
- результат production deploy;
- результат pnpm exec supabase db push;
- подтверждение, что AGENTS.md не затронут.
