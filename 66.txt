Patch 66 — Настройки CRM + отправка PDF-бирок Rozetka Delivery / Meest

ВАЖНО
- Это готовый и отдельно провалидированный Patch 66. Ничего не исследовать, не перепроектировать и не исправлять самостоятельно.
- Не изменять AGENTS.md.
- Не запускать supabase db push: в Patch 66 НЕТ migrations.
- Не применять --include-all.
- Если любая проверка не проходит, остановиться и сообщить точную ошибку.
- Patch 66 подготовлен и повторно проверен против main 087987a0ff4d93a8490aca40e12cfe5ce07d8f94.
- 66.patch теперь НОРМАЛИЗОВАН и содержит все 12 файлов задачи, включая 4 новых файла. Никакие отдельные payload-файлы копировать не нужно.

Рабочая схема
1. Основной проект:
   C:\Users\Вадим\Documents\CRM моя\specmarket
2. В основном проекте:
   git fetch origin
3. Убедиться, что origin/main = 087987a0ff4d93a8490aca40e12cfe5ce07d8f94. Если main уже другой — остановиться и сообщить новый SHA до применения.
4. Работать в отдельном worktree:
   C:\Users\Вадим\Documents\CRM моя\specmarket-p66
   Создать его от origin/main. Если такой путь уже существует, использовать только если worktree чистый и стоит ровно на актуальном origin/main; иначе остановиться.
5. Получить Patch 66 во временный файл без изменения байтов:
   cmd /c "git show origin/chatgpt-patches:66.patch > 66.apply.patch"
6. Проверить SHA-256 файла:
   certutil -hashfile 66.apply.patch SHA256
   Ожидаемый SHA-256:
   3242d2a095c2767d40a3ba3db66abc5f8abb01b04c7716a32d2f006d461bd550
   Если SHA не совпал — остановиться.
7. Выполнить:
   git apply --check 66.apply.patch
   Только при успехе:
   git apply 66.apply.patch
   Затем:
   Remove-Item 66.apply.patch

Файлы Patch 66 — ровно 12
- src/features/orders/types.ts
- src/layouts/MainLayout.vue
- src/pages/HomeView.vue
- src/pages/SettingsView.vue
- src/router/index.ts
- supabase/config.toml
- supabase/functions/_shared/order-label.ts
- supabase/functions/_shared/order-label_test.ts
- supabase/functions/send-order-label-email/index.ts
- supabase/functions/sync-epicentr-orders/index.ts
- supabase/functions/sync-kasta-orders/index.ts
- supabase/functions/sync-prom-orders/index.ts
Никаких других файлов в commit быть не должно.

Что должен сделать Patch 66
- Верхнюю кнопку «Цены» заменить на «Настройки» с шестерёнкой.
- Боковой вертикальный ярлык «ЦЕНЫ» оставить без изменений.
- Добавить /settings:
  - email бирок, default prozaxist.ocean@gmail.com;
  - email хранить в Supabase Auth user_metadata и использовать на всех устройствах того же CRM-аккаунта;
  - звуковые уведомления — локально на устройстве;
  - тема System / Light / Dark — локально на устройстве;
  - переходы в «Цены и себестоимость» и «Курсы валют».
- Убрать отдельный переключатель темы из footer и отдельную кнопку уведомлений из верхней панели.
- Добавить «Отправить бирку» для Rozetka Delivery и Meest, включая Epicentr parcel_box / CVZ / pickup point, которые идут через Meest.
- Пользователь выбирает PDF; Edge Function повторно читает заказ и не доверяет carrier/TTN/email-body из браузера.
- Только PDF до 5 МБ и максимум 5 страниц.
- PDF текст читается через unpdf 1.8.1.
- Rozetka: самостоятельный PRM-... .
- Meest: самостоятельный NNN-NNNNNNN; длинный технический штрихкод вроде 723-1356399001001HSU не считается ТТН.
- PDF ТТН должна совпасть с текущей ТТН заказа; mismatch и отсутствие ТТН блокируют отправку.
- Перед фактической отправкой carrier/TTN заказа перепроверяются ещё раз.
- Subject и body:
    Розетка PRM-...
    Мист NNN-NNNNNNN
- PDF прикладывается к письму.
- recipient: user_metadata.labelRecipientEmail, fallback prozaxist.ocean@gmail.com.
- Отправка через Resend.
- После успешного Resend сохранять в delivery:
    labelEmailSentAt
    labelEmailSentTtn
    labelEmailMessageId
- Отметку не ставить при неуспешной отправке.
- Сохранение отметки защищено optimistic guard по updated_at.
- Если письмо уже ушло, но отметку безопасно сохранить нельзя, функция должна явно вернуть emailSent=true и предупредить не отправлять повторно до проверки почты.
- В карточке показывать «Бирка отправлена» + дата/время.
- Повторная отправка текущей ТТН — только после confirm.
- Если ТТН поменялась, старая отметка не считается отправкой новой ТТН.
- sync-prom-orders, sync-epicentr-orders и sync-kasta-orders сохраняют labelEmail* при последующих sync.
- sync-delivery-tracking не меняется.

Обязательные проверки после применения
1. git status --short
   Проверить: нет AGENTS.md, нет migrations, нет посторонних файлов.
2. git diff --check
3. pnpm format
4. После format восстановить только известный посторонний formatter drift, если он появился без связи с Patch 66:
   git checkout HEAD -- src/features/orders/delivery-tracking.ts
   Никакие другие изменения format не откатывать без причины.
5. pnpm type-check
6. pnpm lint
7. pnpm build
8. Если команда deno доступна локально:
   deno test supabase/functions/_shared/order-label_test.ts
   deno check --node-modules-dir=auto supabase/functions/send-order-label-email/index.ts
   Если deno локально отсутствует — не устанавливать самовольно; отметить это в отчёте. Эти две проверки уже успешно пройдены в GitHub validation Patch 66.
9. Снова:
   git diff --check
   git status --short
   git diff --stat
   git diff
10. Убедиться, что diff содержит ровно 12 файлов из списка выше.

Контрольные случаи
- Rozetka PRM-251996742 => ровно PRM-251996742.
- Meest 723-1356399 => ровно 723-1356399.
- 723-1356399001001HSU не определяется как 723-1356399.
- parcel_box_epicentr и cvz_epicentr => Meest.
- Новая почта не получает кнопку отправки бирки.
- mismatch блокирует send.
- guest@gmail.com блокируется.
- labelEmail* сохраняются только после Resend HTTP success.
- optimistic updated_at guard не перезаписывает параллельно обновлённый delivery.
- marketplace sync сохраняет labelEmail*.

Commit / push
- В commit включить только 12 файлов Patch 66.
- Commit message:
  Add CRM settings and label email sending
- Перед commit/push снова git fetch origin.
- Если origin/main изменился после создания worktree — остановиться и сообщить новый SHA; самостоятельно конфликт не решать.
- Commit и push:
  git add <только 12 файлов Patch 66>
  git commit -m "Add CRM settings and label email sending"
  git push origin HEAD:main

Deploy после успешного push
- pnpm exec supabase functions deploy send-order-label-email
- pnpm exec supabase functions deploy sync-prom-orders
- pnpm exec supabase functions deploy sync-epicentr-orders
- pnpm exec supabase functions deploy sync-kasta-orders
- sync-delivery-tracking не deploy-ить.
- supabase db push НЕ запускать.

Secrets
- Ничего не придумывать и не хардкодить.
- Для фактической отправки production нужны:
  RESEND_API_KEY
  RESEND_FROM_EMAIL
- Если их нет, код и deploy всё равно завершить, а в отчёте явно написать:
  «Отправка бирок требует настройки RESEND_API_KEY и RESEND_FROM_EMAIL».
- Не использовать onboarding@resend.dev как production sender.

Финальный отчёт
Коротко сообщить:
- commit SHA;
- push main success/failure;
- какие проверки прошли;
- deploy send-order-label-email / sync-prom-orders / sync-epicentr-orders / sync-kasta-orders;
- что db push НЕ запускался;
- требуется ли настройка RESEND_API_KEY / RESEND_FROM_EMAIL.
