SpecMarket — Patch 165: reduce avoidable log ingestion WITHOUT reducing sync cadence

Base main SHA: 7e779a680b397ddda6954c3e639bc9b878d88177
Source: chatgpt-patches/165.patch. This patch changes only successful informational console logs in NovaPay and verbose item-level Kasta debug output. NO SQL migration, cron/cadence changes, JWT/refresh retry changes, RLS changes, bank-account data model changes, or disabled error logging.

Actions for Codex:
1. Fetch origin main and chatgpt-patches; check base SHA, working tree and git apply --check; apply 165.patch. Read AGENTS.md. Do not mass-format unrelated files; format touched files only and eliminate formatter churn.
2. Kasta: delete unconditional royalty/item console.log; preserve royalty lookup, cache and item data. NovaPay: remove successful rotation/fingerprint/payment summary output. KEEP credential read-back hash comparison and emit sanitized error on mismatch; preserve retention of NOVAPAY_AUTH_UNCERTAIN, one-use guard, lock, error/warning console logs, safe journal writes, and request_ref persistence BEFORE auth SOAP. Confirm no raw secrets are logged.
3. Run Deno check + existing Deno auth and relevant sync tests; pnpm type-check, scoped Oxlint/ESLint, pnpm build, git diff --check. Report actual results and final diff. Deploy sync-kasta-orders, novapay-data and novapay-balance ONLY if shared auth has changed and balance needs redeployment (it imports shared auth); push main and ensure Production Deploy. Do not claim completion before tests/deploy.
4. READ-ONLY production diagnostics for the main Supabase project (NOT specmarket-monitoring), in SQL Editor:
SELECT name, setting, unit, source, context, pending_restart
FROM pg_settings
WHERE name IN (
  'log_statement','log_min_duration_statement','log_min_messages',
  'log_duration','log_checkpoints','cron.log_statement',
  'log_connections','log_disconnections','log_lock_waits'
)
ORDER BY name;
SELECT rolname, rolconfig
FROM pg_roles
WHERE rolname IN ('postgres','anon','authenticated','service_role');
SELECT d.datname, COALESCE(r.rolname, '(all roles)') AS role_name, s.setconfig
FROM pg_db_role_setting AS s
JOIN pg_database AS d ON d.oid = s.setdatabase
LEFT JOIN pg_roles AS r ON r.oid = s.setrole
WHERE d.datname = 'postgres';
SELECT jobid, jobname, schedule, active FROM cron.job ORDER BY jobid;
Save before-values in deployment report; do not export secrets, SQL text or customer records.
5. Conditional setting decisions, NOT blindly apply: keep log_statement=ddl if already default (DDL audit is useful); if all/mod, propose ddl. Keep log_min_messages=warning and log_min_duration_statement at a threshold that retains slow queries; if changed to debug/notice or 0, propose reducing verbosity. Keep log_checkpoints at its platform default: sparse checkpoint lines are NOT our main driver. Keep error logging, auth audit, PostgreSQL warnings. Never turn off log_min_error_statement, pgAudit or other security requirements.
6. cron.log_statement: if true, this is eligible to disable while cron.job_run_details still records job execution. However Supabase CLI postgres-config update --config cron.log_statement=false --project-ref rtkhgldaswsclkorlyxx --experimental MAY RESTART the production database. DO NOT run it automatically or secretly, and do not leave a pending restart; show current value and request explicit approval for a maintenance restart. This is a separate administrative config step, NOT a SQL migration. Never touch schedules/jobs themselves.
7. Evaluate log type/bytes from Supabase Logs Explorer, preferably per-source over a fixed 24h window. API Gateway edge_logs are automatically produced per request; removing function console output cannot stop them. Do not use dashboard filters or drains as "ingest savings". Compare full-day usage trend 24–48 hours after deploy; no claimed percentage before measurement. No changes to CRM synchronization frequency, payments or NovaPay recovery flag.
8. Report SHA, modified files, tests, deployments, read-only DB settings, deferred config changes requiring approval, and 24–48h comparison plan.

Official docs:
https://supabase.com/docs/guides/platform/manage-your-usage/logs-ingest
https://supabase.com/docs/guides/database/postgres/postgres-log-config
https://supabase.com/docs/guides/database/custom-postgres-config
