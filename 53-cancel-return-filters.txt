Patch 53 — отдельные «Отмены» и «Возвраты». Это продолжение Patch 53, НЕ Patch 54.

Работай механически. Никаких исследований, архитектурного аудита, установки Deno/JDK, дополнительных правок, commit/push/deploy.

Проект:
C:\Users\Вадим\Documents\CRM моя\specmarket

ВАЖНО:
- текущее дерево Patch 53 уже незакоммичено и содержит предыдущие изменения;
- AGENTS.md не трогать;
- ничего не restore/delete;
- этот patch применяется прямо поверх текущего состояния;
- если precheck не проходит, ничего не исправлять самостоятельно: показать ошибку и остановиться.

1. Получи обновлённую ветку:
git fetch origin chatgpt-patches

2. ДО любых изменений проверь patch:
git show origin/chatgpt-patches:53-cancel-return-filters.patch | git apply --check --recount -

Если precheck успешен, только тогда:
git show origin/chatgpt-patches:53-cancel-return-filters.patch | git apply --recount -

3. Выполни только:
git diff --check
pnpm type-check

Deno сейчас НЕ запускать: в текущем PATH его нет. Не устанавливать его и не искать. Android/full build тоже не запускать сейчас.

4. После проверки остановись. Commit/push/deploy запрещены.

В ответ дай только:
- применился ли patch;
- результат git diff --check;
- результат pnpm type-check;
- git status --short;
- при ошибке — точный текст ошибки без самостоятельных исправлений.

Ожидаемая бизнес-логика после patch:
- «Отмены» = заказ отменён до подтверждённого физического движения; из обычного списка исчезает, в «В дороге» не считается, «Принять возврат» не нужен.
- «Возвраты» = заказ уже физически двигался и затем получил отмену/отказ/return-сигнал.
- pending return остаётся в обычном списке и одновременно виден в «Возвраты»; sale/cost/royalty сохраняются.
- partial accepted return остаётся в обычном списке и «Возвраты»; суммы уменьшаются пропорционально.
- full accepted return исчезает из обычного списка, остаётся в «Возвраты», в «В дороге» больше не считается.
- физическое движение определяется только printedAt либо trackingNormalizedStatus accepted/in_transit/ready_for_pickup/delivered/returning/returned; голая ТТН не считается движением.
