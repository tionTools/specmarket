Patch 74 — verified critical audit fixes + Rozetka Delivery fallback

SOURCE OF TRUTH
- Repository: tionTools/specmarket
- Apply only to exact origin/main commit:
  cd1916e37fc71af553a62ed97c3a2b9f6d1deeab
- Patch artifact: origin/chatgpt-patches:74.patch
- Do not touch, stash, reset, clean, or checkout the user's existing dirty worktree at:
  C:\Users\Вадим\Documents\CRM моя\specmarket
- Do not touch AGENTS.md or unrelated local/untracked files.

VERIFIED AUTH ACCOUNTS
Production auth.users preflight on 2026-09-11 showed exactly two CRM accounts:
- owner/write account: user id 80523d58-a9f5-4de0-8751-d57c84a1897a
- guest/read-only account: user id 6c450c97-ac5f-4c80-bcdd-a5cc02128bad
Patch 74 must preserve owner full CRM access and guest read-only access. Any other authenticated account must not gain CRM read/write access or be able to invoke privileged write/sync Edge Functions.

SCOPE
Patch 74 intentionally contains only these fixes:
1. Authorization hardening: add crm_can_write()/crm_can_read() based on the verified auth user IDs, add restrictive RLS guards to existing authenticated public-table policies, and require crm_can_write() for all user-invoked privileged Edge Functions in the repository. Scheduled cron-secret invocations must keep working.
2. Atomic crm_order_items replacement: replace separate DELETE + INSERT calls in save-crm-orders, sync-prom-orders, sync-epicentr-orders and sync-kasta-orders with one PostgreSQL RPC transaction.
3. Prom / Rozetka Delivery fallback: if delivery_provider_data.provider is absent, use the already-resolved carrier/delivery_option value. Recognize labels containing Rozetka while preserving precedence: manual shipping > explicit seller API cost > Rozetka tariff fallback. Add regression coverage for "Магазины Rozetka" and a 105 UAH order => 10 UAH.

OUT OF SCOPE
- Do not change manual-order date handling in this patch; it is not a reconciliation-critical issue.
- Do not change reconciliation/TTN accounting: current business rule is intentional.
- Do not change Android pagination, tracking batching, PricesView persistence, Vue versions, CI/CD, HomeView decomposition, Kasta royalty fallback, Epicentr batching, dark mode, deployment layout, or any Patch53/local artifacts.

REQUIRED WORKFLOW
1. In PowerShell, from the existing repository only fetch refs; do not modify the working tree:
   cd "C:\Users\Вадим\Documents\CRM моя\specmarket"
   git fetch origin main chatgpt-patches

2. Verify exact source before doing anything else:
   git rev-parse origin/main
   It MUST equal:
   cd1916e37fc71af553a62ed97c3a2b9f6d1deeab
   If it differs, STOP. Do not apply or adapt the patch.

3. Create an isolated detached worktree next to the existing project:
   git worktree add --detach "C:\Users\Вадим\Documents\CRM моя\specmarket-patch74" origin/main
   cd "C:\Users\Вадим\Documents\CRM моя\specmarket-patch74"

4. Retrieve the patch artifact without checking out chatgpt-patches:
   git show origin/chatgpt-patches:74.patch > "$env:TEMP\specmarket-74.patch"

5. Validate applicability before changing files:
   git apply --check "$env:TEMP\specmarket-74.patch"
   If this fails, STOP. Do not investigate, adapt, regenerate, or hand-edit the patch.

6. Apply:
   git apply "$env:TEMP\specmarket-74.patch"

7. Verify scope. The diff may contain ONLY these files:
   - supabase/functions/_shared/prom-delivery.ts
   - supabase/functions/_shared/prom-delivery_test.ts
   - supabase/functions/save-crm-orders/index.ts
   - supabase/functions/send-order-label-email/index.ts
   - supabase/functions/sync-delivery-tracking/index.ts
   - supabase/functions/sync-prom-orders/index.ts
   - supabase/functions/sync-epicentr-orders/index.ts
   - supabase/functions/sync-kasta-orders/index.ts
   - supabase/functions/update-order-print-state/index.ts
   - supabase/migrations/20260911130000_atomic_order_items.sql
   - supabase/migrations/20260911131000_crm_authorization_guard.sql

   Run:
   git status --short
   git diff --check
   git diff --stat
   git diff --name-only

   If any other file is changed, STOP and report it. Do not clean or repair unrelated files.

8. Install/check using the existing lockfile:
   corepack enable
   pnpm install --frozen-lockfile
   pnpm type-check
   pnpm build

9. Run focused non-mutating tests/checks where available, including the existing prom-delivery regression test. Do NOT run repository-wide commands with --fix. If Deno is unavailable, report that limitation instead of installing or reconfiguring it.

10. Regression review before commit:
   - Owner account retains CRM read/write access.
   - Guest account retains read access but cannot write, send labels, change print state, or manually launch marketplace/tracking sync.
   - A different authenticated user ID fails crm_can_read()/crm_can_write() guards.
   - Scheduled cron-secret sync calls still bypass user authorization and keep working.
   - Existing manual shipping remains untouched.
   - Explicit Prom seller delivery cost remains higher priority than fallback.
   - "Магазины Rozetka" with order amount 105 resolves to shipping=10 and shippingSource=prom-promo when no manual/API seller cost exists.
   - Order-item replacement sends an empty array correctly and therefore can atomically clear items intentionally.
   - replace_crm_order_items RPC is service-role only; authenticated/anon cannot execute it directly.
   - Prom/Epicentr/Kasta/manual saving preserve the existing item payload/business fields; only transaction boundary changes.

11. Re-check exact final diff and whitespace:
   git diff --check
   git diff --stat
   git diff --name-only

12. Before commit/push, fetch main again and ensure it has not moved:
   git fetch origin main
   git rev-parse origin/main
   It MUST still equal cd1916e37fc71af553a62ed97c3a2b9f6d1deeab.
   If it moved, STOP. Do not rebase or adapt this patch.

13. Commit from the isolated worktree:
   git add supabase/functions/_shared/prom-delivery.ts supabase/functions/_shared/prom-delivery_test.ts supabase/functions/save-crm-orders/index.ts supabase/functions/send-order-label-email/index.ts supabase/functions/sync-delivery-tracking/index.ts supabase/functions/sync-prom-orders/index.ts supabase/functions/sync-epicentr-orders/index.ts supabase/functions/sync-kasta-orders/index.ts supabase/functions/update-order-print-state/index.ts supabase/migrations/20260911130000_atomic_order_items.sql supabase/migrations/20260911131000_crm_authorization_guard.sql
   git commit -m "Fix critical CRM audit findings"

14. Push exactly this detached HEAD to main:
   git push origin HEAD:main

15. Do NOT deploy Supabase migrations or Edge Functions to production automatically as part of this patch application. Report that production activation still requires the normal Supabase migration/function deployment step.

16. Remove only the temporary worktree after a successful push:
   cd "C:\Users\Вадим\Documents\CRM моя\specmarket"
   git worktree remove "C:\Users\Вадим\Documents\CRM моя\specmarket-patch74"

FINAL REPORT
Return:
- applied Patch 74: yes/no
- source main SHA
- git apply --check result
- changed-file list
- type-check result
- build result
- focused test/check result or explicit limitation
- commit SHA
- push result
- confirmation that the original dirty worktree and AGENTS.md were untouched
- reminder that Supabase migration/functions are not production-deployed by this workflow
