Patch 156 — уменьшить Supabase Log Ingestion без потери синхронизации

Repo: tionTools/specmarket
Base main SHA: ab5291a814e49b2fa0004b3526ace6692dfdc147
Patch: origin/chatgpt-patches/156.patch
Expected patch blob SHA: 20fe172e3fa02c1801126afa1bebf6830e07d9d5

Задача: сократить пустые ежеминутные pg_cron-запуски трёх маркетплейсов и отдельные записи статуса каждой доставки. НЕ менять фактическую частоту получения заказов, правила/статусы перевозчиков или схему долга поставщику. Логи ошибок сохранять. Нельзя просто удалить или отключить логирование/cron.

Что в патче:
1. Новая SQL-миграция 20260925130000_marketplace_cron_five_minute_checks.sql: вместо трёх ежеминутных заданий оставить три задания с минутами 0,5,…; 1,6,…; 2,7,… соответственно Prom / Эпицентр / Kasta. Сохранить crm_marketplace_sync_is_due и его проверку рабочего времени Киева: днём раз в 5 минут, ночью раз в час. Не допустить параллельных старых заданий или дублирования.
2. sync-delivery-tracking/worker.ts и state-batch.ts: пакетная запись crm_delivery_tracking_state группами до 25 вместо индивидуальной записи на каждый проверенный заказ. При ошибке пакетной записи повторить поштучно, сохранить ошибки в логах и счётчике failed. Существующие проверки sameShipment, trackingChanged, mergeTrackingDelivery, порядок обновления crm_orders и обработки возвратов сохраняются.
3. state-batch_test.ts: три теста — размеры пакетов, fallback при ошибке и отсутствие запроса для пустой очереди.

Порядок работы Codex:
- Проверь актуальный main и рабочее дерево. git apply --check, затем применить патч; при изменении базы патча остановиться и согласовать реальный diff, не стирать чужие правки.
- Проверь cron-выражения на сохранение дневной частоты и одного запуска в час ночью, корректность Europe/Kyiv и переходов летнего/зимнего времени. Учитывай существующую перегрузку функции crm_marketplace_sync_is_due(text) / (text,timestamptz): НЕ возвращай проблему неоднозначного вызова.
- Проверь поведение worker при API-ошибке, смене ТТН в ходе проверки, отсутствии изменений и частичной ошибке сохранения состояния. Избегай лишних изменений в рабочем carrier-коде. Проверь, что массовое сохранение состояния не создаёт лишних обновлений crm_orders / realtime событий.
- Форматируй только изменённые файлы; перед публикацией и commit проверь diff, без formatter churn. Запусти deno test для state-batch_test.ts и существующих тестов sync-delivery-tracking, доступные проверки Deno/TypeScript, pnpm type-check, scoped Oxlint/ESLint, git diff --check. Если доступен локальный Supabase, проверь миграцию через supabase db reset (все миграции, с учётом известных старых миграций); проверь SQL на fresh и existing DB. Если что-то недоступно — точно сообщи, не называй PASS.
- После успешных проверок commit только этих 4 файлов и SSH push main. Выполни реальный deploy ТОЛЬКО Edge Function sync-delivery-tracking, плюс безопасно примени SQL-миграцию в связанный production Supabase по принятому процессу проекта; НЕ deploy иных функций, не менять другие права/GRANT/RLS. Старые миграции, отсутствующие в таблице истории, не применять заново; migration repair использовать только на основании проверенного состояния. Не трогать вручную cron, пока миграция не проверена.
- Проверь на production SELECT jobid, jobname, schedule, command FROM cron.job WHERE jobname IN ('crm-sync-prom-poll','crm-sync-epicentr-poll','crm-sync-kasta-poll'); ожидать ровно 3 записи и новые выражения. Проверить, что регулярная синхронизация продолжает получать заказы, а доставочный worker работает без ошибок. Сравни количество логов/cron запусков после применения; месячный Usage не обнулится, эффект оценивается по новым событиям.
- Сообщи кратко: SHA commit, итоги тестов, Edge deploy, применённая migration/version, проверенные cron job names/schedules, подтверждение работоспособности и ссылку на Production Deploy (если был). Не заявляй production success до фактической проверки.

Важно: frontend в патче не меняется; обычный frontend Production Deploy может стартовать на push, но это не заменяет Edge deploy и SQL migration. Не реализуй здесь напоминания о бирках 5/10/15 — это отдельная задача.
