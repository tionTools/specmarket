Перейди в локальный проект:
C:\Users\Вадим\Documents\CRM моя\specmarket

Примени Patch 31 из ветки chatgpt-patches полностью.

Цель: изменить только расписание фоновой проверки статусов доставки:
- 07:00–08:59 Europe/Kyiv — как сейчас, не чаще чем раз в 30 минут;
- 09:00–15:59 — раз в 15 минут;
- 16:00–20:59 — как сейчас, раз в 30 минут;
- 21:00–23:59 — как сейчас, раз в 60 минут;
- 00:00–06:59 — как сейчас, автоматическую проверку пропускать.

Patch должен затронуть только:
- supabase/functions/sync-delivery-tracking/worker.ts
- новую миграцию supabase/migrations/20260903154500_delivery_tracking_15_minute_window.sql

Не изменяй, не откатывай, не добавляй в stage и не включай в commit пользовательский AGENTS.md.

Порядок:
1. Убедись, что находишься в указанном репозитории.
2. Получи 31.patch из ветки chatgpt-patches и выполни git apply --check.
3. Примени патч.
4. Проверь статически, что логика интервалов соответствует ровно расписанию выше и другие перевозчики/статусы/обработка tracking не изменены.
5. Выполни:
   pnpm format
   pnpm type-check
   pnpm lint
   pnpm build
   git diff --check
6. Примени новую Supabase migration к связанному production-проекту штатным способом проекта, чтобы cron crm-delivery-tracking реально начал вызываться каждые 15 минут. Не редактируй старую уже применённую миграцию вместо новой.
7. Задеплой только Edge Function sync-delivery-tracking, потому что изменён её worker.ts.
8. Если все проверки и применение инфраструктуры успешны, закоммить только два файла этой задачи с сообщением:
   Increase daytime delivery tracking frequency
9. Push commit в origin/main.

В отчёте укажи:
- полный SHA commit;
- результат push;
- результат применения migration;
- результат deploy sync-delivery-tracking;
- результаты всех проверок;
- итоговый git status --short.

Если что-либо не проходит, не скрывай ошибку и не включай посторонние файлы в commit.