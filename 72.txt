Patch 72 — Web CRM: реестр платежей не должен скрываться старым поиском + мгновенная кнопка «Наверх»

ВАЖНО: основной локальный worktree сейчас грязный и отстаёт от origin/main. Его НЕ трогать, НЕ чистить, НЕ reset-ить и НЕ stash-ить. Все существующие локальные файлы/изменения, включая AGENTS.md и незавершённые Patch53-артефакты, оставить как есть.

Основной локальный репозиторий:
C:\Users\Вадим\Documents\CRM моя\specmarket

Источник истины для этого Patch: origin/main commit
7cb7bff3ec45c46009913877b95b7b9e6b9b0572

Patch 72 уже проверен на этом exact origin/main. Работай через отдельный чистый git worktree, чтобы не затрагивать основной грязный worktree.

Цель Patch:
1. Если перед загрузкой реестра платежей был активен контекстный поиск, после успешной загрузки реестра поисковая строка должна очищаться. Реестр должен показываться без маскировки старым searchQuery.
2. Кнопку «Наверх» переместить с нижнего левого угла к вертикальному центру левого края экрана.
3. Переход наверх должен быть мгновенным, без smooth-scroll.
4. Не менять бизнес-логику реестров, финансов, Supabase и Android.

Выполни полностью:

1. В основном репозитории только обнови remote refs, не меняя рабочее дерево:
   cd "C:\Users\Вадим\Documents\CRM моя\specmarket"
   git fetch origin

2. Проверь именно origin/main:
   git rev-parse origin/main
   Должно быть ровно:
   7cb7bff3ec45c46009913877b95b7b9e6b9b0572
   Если origin/main уже другой — STOP, Patch не применять и сообщить новый SHA.

3. Не требуй чистоты основного worktree и ничего в нём не изменяй. Зафиксируй только для отчёта текущее `git status --short`, но не выполняй checkout/reset/clean/stash/pull в основном worktree.

4. Создай отдельный чистый временный worktree рядом с проектом:
   $repo = "C:\Users\Вадим\Documents\CRM моя\specmarket"
   $wt = "C:\Users\Вадим\Documents\CRM моя\specmarket-patch72"
   if (Test-Path $wt) { throw "STOP: $wt already exists" }
   git -C $repo worktree add --detach $wt origin/main

5. Получи Patch 72 и примени его только во временном worktree:
   git -C $repo fetch origin chatgpt-patches
   git -C $repo show origin/chatgpt-patches:72.patch > "$wt\72.local.patch"
   git -C $wt apply --check 72.local.patch
   git -C $wt apply 72.local.patch
   Remove-Item "$wt\72.local.patch"

6. Сразу проверь scope во временном worktree:
   git -C $wt diff --check
   git -C $wt diff --name-only
   В diff должен быть только:
   src/pages/HomeView.vue
   AGENTS.md и любые Patch53-файлы не должны попасть в diff.

7. Статически проверь три изменения в src/pages/HomeView.vue:
   - в handlePromRegistryFile после успешного создания draft есть: searchQuery.value = ''
   - scrollOrdersToTop использует behavior: 'auto', а не 'smooth'
   - у кнопки «Наверх» позиционирование содержит top-1/2 и -translate-y-1/2; bottom-5 у этой кнопки отсутствует.

8. Запусти проверки только во временном worktree:
   cd $wt
   pnpm install --frozen-lockfile
   pnpm format
   Если formatter изменит src/features/orders/delivery-tracking.ts только из-за известного formatter drift — восстанови только этот файл из HEAD и не включай его в Patch.
   pnpm type-check
   pnpm lint
   pnpm build

9. После проверок снова:
   git diff --check
   git diff --name-only
   git diff -- src/pages/HomeView.vue
   В итоговом diff должен остаться только src/pages/HomeView.vue. Любые другие tool-induced изменения во временном worktree восстанови.

10. Перед commit/push ещё раз проверь, что remote main не сдвинулся:
    git -C $repo fetch origin
    git -C $repo rev-parse origin/main
    Должно оставаться:
    7cb7bff3ec45c46009913877b95b7b9e6b9b0572
    Если SHA другой — STOP без commit/push.

11. Не открывай production/localhost и не обходи логин. Supabase Functions deploy НЕ запускать. supabase db push НЕ запускать. Миграций в Patch нет.

12. Commit/push только файла задачи из временного worktree:
    cd $wt
    git add src/pages/HomeView.vue
    git commit -m "Fix registry search and scroll-to-top"
    git push origin HEAD:main

13. После успешного push удали только временный worktree:
    cd $repo
    git worktree remove $wt
    Основной worktree и все его старые локальные изменения должны остаться нетронутыми.

14. В отчёте укажи:
    - commit SHA;
    - push в main успешен/нет;
    - итоговый список файлов commit;
    - результаты pnpm format, pnpm type-check, pnpm lint, pnpm build;
    - что основной грязный worktree не изменялся и его локальные файлы сохранены;
    - что AGENTS.md не изменялся;
    - что Supabase Functions deploy и db push не запускались.
