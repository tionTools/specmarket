Patch 78 — время последней смены статуса доставки под плашкой

База main:
b43697312b10c77d782962b7cf5e7a3ca4a0c6d5

Что меняется:
1. В списке заказов под плашкой «Состояние отгрузки» показывается время последней фактической смены статуса доставки из delivery.trackingStatusChangedAt.
2. Формат: HH:MM DD.MM.YYYY в часовом поясе Europe/Kyiv, например 17:05 14.09.2026.
3. Время центрируется строго относительно плашки статуса, а не прижимается вправо.
4. Время вынесено через absolute positioning, поэтому не добавляет отдельную строку в grid и не увеличивает высоту карточки заказа.
5. Если trackingStatusChangedAt отсутствует или невалиден, время не показывается.

Scope:
- только src/pages/HomeView.vue
- AGENTS.md не трогать
- Supabase не менять
- Android не менять

Предварительно проверено ChatGPT:
- актуальный main = b43697312b10c77d782962b7cf5e7a3ca4a0c6d5
- 77 уже занят; выбран свободный Patch 78
- patch context соответствует текущему HomeView.vue
- форматтер времени использует Europe/Kyiv
- diff Patch 78 содержит только изменение HomeView.vue
- package.json: pnpm format = `oxfmt src/`, поэтому format может автоматически тронуть уже существующие файлы вне scope; такие formatter-only spillover изменения НЕ входят в Patch 78 и должны быть восстановлены из HEAD
- pnpm lint также запускает fix-режимы по проекту, поэтому после lint нужно так же убрать любые изменения вне scope

Codex — продолжение безопасно выполнять в отдельном чистом worktree Patch 78. Основной worktree C:\Users\Вадим\Documents\CRM моя\specmarket не трогать вообще: не stash, не reset, не checkout, не restore, не clean; его AGENTS.md и untracked пользовательские файлы оставить как есть.

Если отдельный worktree уже создан и Patch 78 уже применён, НЕ начинай заново. Продолжи с текущего состояния по шагам ниже.

1. Убедись, что работа идёт в отдельном worktree Patch 78, созданном от origin/main = b43697312b10c77d782962b7cf5e7a3ca4a0c6d5.

2. Текущее допустимое состояние после pnpm format:
- изменён src/pages/HomeView.vue — это Patch 78;
- могут появиться formatter-only изменения других файлов src, в частности src/features/orders/delivery-tracking.ts.

3. После pnpm format восстанови ВСЕ изменения вне scope Patch 78 из HEAD. Например для текущего известного spillover:
   git restore --source=HEAD -- src/features/orders/delivery-tracking.ts

Если format изменил ещё какие-либо файлы кроме src/pages/HomeView.vue — восстанови каждый такой файл из HEAD. Не восстанавливай HomeView.vue.

4. Проверь:
   git diff --check
   git diff --name-only

После восстановления formatter spillover должен остаться ровно один изменённый файл:
   src/pages/HomeView.vue

Если остаётся что-то ещё — остановись и сообщи список.

5. Выполни:
   pnpm type-check

Если падает — ничего не исправлять самостоятельно; остановиться и прислать точную ошибку.

6. Выполни:
   pnpm lint

ВАЖНО: pnpm lint содержит --fix и может снова изменить файлы вне scope. После успешного lint сразу проверь:
   git diff --name-only

Если lint изменил файлы вне src/pages/HomeView.vue, восстанови ТОЛЬКО эти out-of-scope файлы из HEAD:
   git restore --source=HEAD -- <out-of-scope-file> ...

HomeView.vue не восстанавливать.

7. После восстановления lint spillover снова проверь:
   git diff --check
   git diff --name-only

Должен остаться ровно:
   src/pages/HomeView.vue

8. Выполни:
   pnpm build

Если падает — ничего не исправлять самостоятельно; остановиться и прислать точную ошибку.

9. Проверь бизнес-логику по итоговому HomeView.vue:
- timestamp выводится только при валидном trackingStatusChangedAt;
- используется Europe/Kyiv;
- формат HH:MM DD.MM.YYYY;
- timestamp центрирован через left-1/2 + -translate-x-1/2 относительно контейнера плашки;
- timestamp расположен absolute top-full и не создаёт отдельную grid-строку;
- сама status-плашка сохраняет прежний line-clamp-2;
- никакая логика заказов, доставки, Supabase или Android не изменена.

10. Финальный scope-gate перед commit:
   git diff --check
   git diff --name-only
   git status --short

В tracked diff должен быть ровно один файл:
   src/pages/HomeView.vue

11. Перед commit:
   git fetch origin main
   git rev-parse origin/main

origin/main всё ещё должен быть:
   b43697312b10c77d782962b7cf5e7a3ca4a0c6d5

Если main продвинулся — не rebase, не merge и не адаптировать самостоятельно; остановиться и сообщить новый SHA.

12. Если всё зелёное, commit только src/pages/HomeView.vue и push в main через существующий SSH-доступ GitHub. Не использовать git add .; не переключаться на HTTPS; не запускать browser auth; не создавать credentials.

13. Supabase deploy не нужен.

14. После push дождись обычного frontend deploy через GitHub Actions и сообщи:
- SHA commit в main;
- результаты pnpm format / pnpm type-check / pnpm lint / pnpm build;
- какие formatter/lint spillover-файлы были восстановлены из HEAD;
- подтверждение, что итоговый commit содержит только src/pages/HomeView.vue;
- deploy status;
- подтверждение, что основной грязный worktree, AGENTS.md и untracked пользовательские файлы не затронуты.
