SpecMarket — Patch 161: компактная кнопка «Повторить заказ»
Base main SHA: 8ea11ed3f563b14a3280668c69a994f5bbc2334b
Изменяемый файл: src/pages/HomeView.vue.
Файлы: chatgpt-patches/161.patch и chatgpt-patches/161.txt.

В развёрнутой карточке заказа удалить большую текстовую кнопку «Повторить заказ» из правой группы действий. Вместо неё поставить слева, в самом начале ряда кнопок перед «1С»/«Excel», компактную иконку Copy (32 × 32 px). Tooltip и aria-label должны обозначать повтор заказа, а не копирование номера или строки Excel. Не менять обработчик openRepeatOrderDialog, логику клонирования, сохранения, отправки и синхронизации заказов. Использовать @click.stop, чтобы нажатие не затрагивало поведение карточки.

Codex:
1. git fetch origin main chatgpt-patches; проверить актуальный origin/main. Если main изменился после base, проверить конкретный diff HomeView.vue и перенести только минимальное UI-изменение без наложения поверх чужих работ.
2. git apply --check 161.patch; git apply 161.patch.
3. Форматирование только изменённого HomeView.vue; убрать formatter churn. Проверить pnpm type-check, scoped oxlint/eslint, pnpm build, git diff --check; реальный diff должен содержать только перемещение/уменьшение кнопки.
4. Ручная проверка: кнопка видна слева в раскрытом заказе на десктопе и узком экране; по нажатию открывается новый черновик Р/С с прежним содержимым, оригинал не изменяется; 1С и Excel не затронуты; guest не видит кнопку.
5. После проверок — commit, SSH push, production deploy по стандартным правилам. Сообщить commit SHA и ссылку на deploy.
