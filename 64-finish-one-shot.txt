ФИНАЛЬНОЕ ОДНОРАЗОВОЕ ЗАВЕРШЕНИЕ PATCH 64.

Работай только с уже применённым Patch 64 в локальном проекте:
C:\Users\Вадим\Documents\CRM моя\specmarket

Цель: в одной итерации довести Patch 64 до commit/push и успешного GitHub Actions. Не останавливайся на исправимых локальных инфраструктурных проблемах Windows/Android. Сам устраняй их в рамках правил ниже. Остановиться можно только на реальной ошибке кода/тестов/подписи, невозможности доступа к GitHub либо если требуется действие пользователя, которое технически невозможно выполнить самостоятельно.

ТЕКУЩЕЕ СОСТОЯНИЕ, КОТОРОЕ НЕЛЬЗЯ ЛОМАТЬ:
- Patch 64 уже применён. НЕ применяй 64.patch повторно.
- Working tree НЕ сбрасывай.
- Keystore уже создан и проверен вне репозитория. НЕ регенерируй его.
- Все 4 GitHub release Secrets уже настроены. НЕ удаляй и НЕ перезаписывай их.
- GitHub CLI уже установлен и авторизован.
- Temurin JDK 21 уже установлен.
- Android CLI установлен; бинарник: C:\Users\Вадим\AppData\AndroidCLI\android.exe
- В C:\Users\Public\Android\Sdk может существовать ЧАСТИЧНО распакованный/повреждённый пакет platforms\android-36 после DirectoryNotEmptyException. Это НЕ причина останавливаться.
- Commit Patch 64 должен содержать только 6 файлов:
  1. .github/workflows/android-apk.yml
  2. android/app/build.gradle.kts
  3. android/app/src/main/java/ua/orders/crm/Appearance.kt
  4. android/app/src/main/java/ua/orders/crm/MainActivity.kt
  5. android/app/src/main/java/ua/orders/crm/OrderModels.kt
  6. android/app/src/test/java/ua/orders/crm/OrderModelsTest.kt

1. ПРОВЕРЬ DIFF
Выполни `git diff --check`, `git diff --name-only`, `git diff --stat`.
Посторонние локальные изменения не трогай и не включай в commit.

2. ПОСТОЯННЫЕ ASCII-ПУТИ
Создай writable каталоги:
- C:\dev\gradle-home
- C:\dev\tmp
- C:\Users\Public\Android\Sdk

В текущем процессе установи:
- GRADLE_USER_HOME=C:\dev\gradle-home
- TEMP=C:\dev\tmp
- TMP=C:\dev\tmp
- ANDROID_HOME=C:\Users\Public\Android\Sdk
- ANDROID_SDK_ROOT=C:\Users\Public\Android\Sdk

ANDROID_HOME и ANDROID_SDK_ROOT также установи как User environment variables Windows.

Для проекта используй временный ASCII-path через свободную букву диска (`T:` предпочтительно) с `subst` на текущий repo. Не копируй репозиторий. Все Gradle-команды запускай из `<drive>:\android`.

3. JDK
Найди уже установленный Temurin JDK 21 через `where.exe java`, `Get-Command java`, стандартные каталоги Adoptium или реестр Windows. Убедись, что `java -version` работает. Если JAVA_HOME не задан — задай его на найденный JDK 21. Новый JDK НЕ устанавливай.

4. ANDROID SDK — ДОВЕДИ ДО РАБОЧЕГО СОСТОЯНИЯ
Используй уже найденный CLI:
C:\Users\Вадим\AppData\AndroidCLI\android.exe

Bare/help exit code не является критерием работоспособности. Сначала можно выполнить `android.exe --version`, затем попытаться:
`android.exe --sdk=C:\Users\Public\Android\Sdk sdk install platforms/android-36 build-tools/36.0.0 platform-tools`

Критерий успеха — фактическое наличие:
- C:\Users\Public\Android\Sdk\platforms\android-36\android.jar
- C:\Users\Public\Android\Sdk\build-tools\36.0.0\apksigner.bat
- C:\Users\Public\Android\Sdk\platform-tools

ВАЖНО — RECOVERY ПОСЛЕ ЧАСТИЧНОЙ УСТАНОВКИ / DirectoryNotEmptyException:
Если официальный Android CLI падает с `DirectoryNotEmptyException`, `LocalSdk.deleteRecursively`, ошибкой удаления/замены каталога либо пакет существует частично, НЕ ОСТАНАВЛИВАЙСЯ.

Для каждого требуемого пакета сначала определи, полноценен ли он по контрольному файлу:
- platform 36 полноценен только если существует `platforms\android-36\android.jar`;
- build-tools 36.0.0 полноценен только если существует `build-tools\36.0.0\apksigner.bat`.

Если каталог существует, но контрольного файла нет — это повреждённая незавершённая установка. Удали ТОЛЬКО соответствующий повреждённый пакет SDK, не весь SDK:
- `C:\Users\Public\Android\Sdk\platforms\android-36`
- при необходимости `C:\Users\Public\Android\Sdk\build-tools\36.0.0`
- при необходимости незавершённый временный каталог того же пакета внутри SDK, если он явно создан установщиком и мешает повторной установке.

Сначала попробуй `Remove-Item -Recurse -Force`. Если Windows удерживает каталог/остатки, заверши только процессы Android CLI/sdkmanager/java, относящиеся к этой неудачной установке, затем повтори удаление. При необходимости используй `cmd /c rmdir /s /q` для ТОЧНОГО повреждённого каталога. Не удаляй keystore, credentials, проект, Gradle cache или весь Android SDK.

После очистки повреждённого пакета НЕ трать время на бесконечные повторы Android CLI. Переходи на официальный Google Command-line Tools + sdkmanager.

OFFICIAL SDKMANAGER FALLBACK:
- скачай официальный Windows package Android SDK Command-line Tools с dl.google.com;
- распакуй его в `C:\Users\Public\Android\Sdk\cmdline-tools\latest` так, чтобы существовал `C:\Users\Public\Android\Sdk\cmdline-tools\latest\bin\sdkmanager.bat`;
- если `sdkmanager.bat` уже существует и запускается, повторно command-line tools не скачивай;
- прими необходимые Android SDK licenses;
- затем установи ЯВНО с `--sdk_root=C:\Users\Public\Android\Sdk`:
  `platforms;android-36`, `build-tools;36.0.0`, `platform-tools`.

После sdkmanager снова проверь контрольные файлы. Если они существуют, считай SDK установленным независимо от прежних ошибок Android CLI.

Используй только официальные инструменты Google. Android Studio, emulator, system images, NDK и CMake НЕ устанавливай.

5. RELEASE SIGNING
Используй только существующий keystore и credentials вне repo в соседнем каталоге `android-signing`. Ничего не регенерируй и не выводи секретные значения. Загрузи credentials в:
- ORDERS_RELEASE_STORE_FILE
- ORDERS_RELEASE_STORE_PASS
- ORDERS_RELEASE_ALIAS
- ORDERS_RELEASE_KEY_PASS

Через `gh secret list --repo tionTools/specmarket` убедись только в наличии 4 release Secrets. Значения не выводи.

6. ЛОКАЛЬНАЯ СБОРКА
Из ASCII-path `<drive>:\android` выполни:
`.\gradlew.bat --no-daemon testDebugUnitTest lintDebug assembleDebug assembleRelease --stacktrace`

Если появляется локальная Windows/sandbox ошибка `Unable to establish loopback connection`, повтори один раз через обычный системный `powershell.exe` или `cmd.exe`, сохранив те же JAVA_HOME, ANDROID_HOME, ANDROID_SDK_ROOT, GRADLE_USER_HOME, TEMP/TMP и ASCII-path.

Если после этого единственная причина невозможности локального запуска — sandbox/loopback инфраструктура Codex, НЕ БЛОКИРУЙ Patch 64: exact Patch 64 уже отдельно проверялся на GitHub runner с tests/lint/debug/release/apksigner. В этом случае разрешено перейти к commit/push, но GitHub Actions становится обязательным финальным gate.

Если Gradle доходит до проекта и падает на реальной compile/test/lint/signing ошибке — остановись и сообщи точную ошибку. Код Patch 64 самостоятельно не меняй.

7. ПРОВЕРКА RELEASE APK ЛОКАЛЬНО
Если assembleRelease успешен:
`C:\Users\Public\Android\Sdk\build-tools\36.0.0\apksigner.bat verify --print-certs android\app\build\outputs\apk\release\app-release.apk`

8. COMMIT/PUSH
Перед commit ещё раз проверь:
- `git diff --check`
- `git diff --name-only`
- `git diff --stat`

Commit должен содержать ровно 6 файлов Patch 64 и никаких инфраструктурных файлов/credentials/local.properties.
Commit message:
`Polish Android orders app and stabilize release updates`

Push в `origin/main`.

9. GITHUB ACTIONS — ОБЯЗАТЕЛЬНЫЙ ФИНАЛЬНЫЙ GATE
После push найди Android APK workflow для нового main commit и доведи его до success.
Workflow обязан:
- выполнить tests/lint;
- собрать release APK;
- выполнить `apksigner verify`;
- создать artifact `orders-android-release`.

Если workflow падает — прочитай точный failed step/log. Если причина чисто инфраструктурная в workflow и исправляется без изменения бизнес-кода Patch 64, исправь workflow в рамках Patch 64 и доведи run до success. Если требуется изменение Android-кода/логики — остановись и сообщи точную ошибку.

После успешного run скачай `orders-android-release` через gh и повторно проверь APK `apksigner verify --print-certs`. Зафиксируй SHA-256 сертификата подписанта.

10. ЗАВЕРШЕНИЕ
Удалить только временный `subst` mapping. НЕ удалять Android SDK, Temurin JDK, Gradle cache, GitHub CLI, keystore/credentials или GitHub Secrets. Это постоянная Android build-инфраструктура.

Финальный отчёт — только факты:
- SHA commit в main;
- какие локальные проверки реально прошли и был ли fallback GitHub Actions из-за sandbox loopback;
- Android SDK path;
- GitHub Actions run ID/result;
- artifact `orders-android-release`;
- SHA-256 сертификата подписанта;
- подтверждение, что commit содержит только 6 файлов Patch 64;
- напоминание: переход 0.4 -> 0.5 требует один раз удалить старую версию, установить 0.5 и войти; дальнейшие версии с тем же release key и растущим versionCode должны обновляться поверх приложения.

НЕ ОСТАНАВЛИВАЙСЯ на исправимых вещах: PATH, env-переменные, writable cache, отсутствующий/частично распакованный SDK package, DirectoryNotEmptyException при SDK install, help/non-zero Android CLI, fallback на sdkmanager, новый shell, ASCII subst. Эти вещи исправляй сам и продолжай до результата.