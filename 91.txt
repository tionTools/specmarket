Patch 91 — Android FCM push notifications

Base main: bfeb68e0cd586d38bf6594e939537e9f6e79cea8
Patch blob: d94183df77eabe6fa4d12d1e35f4f77c482bca06
Android: versionName 0.15, versionCode 15
Firebase project: specmarket-orders
Android package: ua.orders.crm

Goal:
A new marketplace order must produce an Android notification even when the normal app process is not running. Existing Supabase Realtime remains the live synchronization channel while the process is alive. Tapping the push notification must open that exact order through the Patch 89 deep-link path.

Scope — exactly 14 files:
- android/app/build.gradle.kts
- android/app/google-services.json
- android/app/src/main/AndroidManifest.xml
- android/app/src/main/java/ua/orders/crm/OrderNotification.kt
- android/app/src/main/java/ua/orders/crm/OrdersRepository.kt
- android/app/src/main/java/ua/orders/crm/OrdersViewModel.kt
- android/app/src/main/java/ua/orders/crm/PushMessaging.kt
- android/app/src/test/java/ua/orders/crm/PushMessagingTest.kt
- android/build.gradle.kts
- supabase/functions/_shared/firebase-messaging.ts
- supabase/functions/_shared/firebase-messaging_test.ts
- supabase/functions/manage-push-device/index.ts
- supabase/functions/send-order-push/index.ts
- supabase/migrations/20260916160000_order_push_notifications.sql

What changes:
- Adds Firebase Cloud Messaging to Android using google-services.json for ua.orders.crm.
- Registers a stable per-install device id and the current FCM token after Supabase authentication.
- Notification permission and the existing "new order notifications" setting also control server push registration.
- Logout removes the device registration before signing out when possible and disables local push display immediately.
- Adds FirebaseMessagingService for data messages so Android can build the same local notification and preserve the exact-order PendingIntent from Patch 89.
- FCM and Realtime use the same order-id notification identity plus a persistent 7-day order-id dedupe guard, so one order does not generate two alerts when both channels deliver it.
- Keeps existing crm:orders / order_changed Realtime behavior unchanged.
- Adds crm_push_devices for device tokens. Direct public/anon/authenticated table access is revoked; Edge Functions use service-role access.
- Adds manage-push-device, which manually validates the current Supabase user and rejects guest@gmail.com.
- Adds send-order-push, authenticated internally by the existing crm_sync_cron_secret, which obtains a Firebase HTTP v1 OAuth token from the configured Firebase service account and sends a high-priority data push.
- Adds AFTER INSERT trigger on crm_orders. The trigger queues send-order-push through pg_net. It only acts on inserts; status updates do not create a new-order push.
- send-order-push only notifies for Prom/Epicentr/Kasta new-order candidates without TTN/cancel/return signals and removes unregistered FCM tokens.

Secrets already configured in Supabase by the user; never put their values in Git or logs:
- FIREBASE_PROJECT_ID
- FIREBASE_CLIENT_EMAIL
- FIREBASE_PRIVATE_KEY

Deployment order after all local checks pass:
1. Deploy `manage-push-device` with `--no-verify-jwt` because the function validates the bearer token itself with Supabase Auth.
2. Deploy `send-order-push` with `--no-verify-jwt` because it validates the internal crm_sync_cron_secret itself.
3. Only after both functions are deployed, apply the migration/database push so the new INSERT trigger never points to a missing function.
4. Commit/push main through the existing GitHub SSH flow. Do not use HTTPS/browser auth/new credentials.
5. Wait for GitHub Actions `Android APK`, require SUCCESS and signed APK verification.

Required pre-apply checks:
- fetch origin/main and origin/chatgpt-patches;
- origin/main must still equal bfeb68e0cd586d38bf6594e939537e9f6e79cea8; if not, stop and report, do not manually adapt Patch 91;
- `git rev-parse origin/chatgpt-patches:91.patch` must equal d94183df77eabe6fa4d12d1e35f4f77c482bca06;
- working tree must be clean; do not reset/stash unrelated user work automatically;
- run `git show origin/chatgpt-patches:91.patch | git apply --check -`; failure => stop, no manual adaptation;
- apply the same stream only after the check passes;
- run `git diff --check`, `git diff --name-only`, and `git status --short`; scope must be exactly the 14 files above.

Required checks after apply:
- run `deno test supabase/functions/_shared/firebase-messaging_test.ts`;
- run Deno type/check validation for firebase-messaging.ts, manage-push-device/index.ts and send-order-push/index.ts if the project Deno tooling is available;
- run the standard `scripts/android-check.ps1` harness only; do not create a new JDK/Gradle cache/junction/build environment;
- Android harness must run testDebugUnitTest, lintDebug and assembleRelease; SUCCESS_LOCAL_CI_SIGNING_REQUIRED is acceptable locally when signing credentials are intentionally absent;
- re-run `git diff --check` and exact scope verification after checks.

Commit message:
Add Android FCM order push notifications

Real-device acceptance test after production deploy and APK install:
- launch/login once so the phone registers its FCM token;
- confirm notifications are enabled and Android notification permission is granted;
- move the app to background, then remove it from recents / allow Android to kill the normal process; do NOT use Force stop;
- receive a real new Prom/Epicentr/Kasta order;
- notification must arrive without opening the app;
- tapping it must open the exact order;
- while the app process is alive, Realtime must still update orders and the same order must not alert twice.

Known Android platform boundary:
If the user explicitly Force stops the app, Android can block FCM delivery until the app is manually launched again.

Do not modify AGENTS.md, frontend, existing marketplace sync logic, Patch 90 files, or unrelated files.